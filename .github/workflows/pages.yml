name: Marrakech Bongo Preview Status

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  status:
    runs-on: ubuntu-latest
    steps:
      - name: Preview publishing status
        run: |
          echo "GitHub Pages publishing is intentionally disabled for this repository."
          echo "The live Marrakech Bongo / ОБРАЗ platform is deployed through the existing Beget/n8n production infrastructure."
          echo "This workflow is manual-only and does not deploy or modify production."
